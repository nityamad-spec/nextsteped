Insert the four missing CMR universities into the `universities` table so they appear in the `UniversityCombobox` on `/student/onboarding`:

- CMR College of Engineering & Technology
- CMR Engineering College
- CMR Institute of Technology
- CMR Technical Campus

No code changes — the combobox already loads all rows from `universities` ordered by name, so adding them via a data insert is sufficient.